#pickle模块像超级通用的数据格式化和解析工具。
#如果你真的想储存Pyhon原生对象，但又无法信赖文件和数据来源，Python标准库pickle模块会是个理想的选择。
#

特别注意：
    pickle 并不安全，它会潜在地执行 unpickle 的对象，所以你应该永远不要用 pickle 去读取从非信任源获取的数据。
    但是对于可控环境中的本地对象持久化，它简单而且高效。如果在自己的项目中使用 pickle， 应该意识到你可能遇到一些
     pickle 特有的异常，这样你可能想要用 try/except 结构体来包装你的代码。
