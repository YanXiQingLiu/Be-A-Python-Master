 1.
 Python 中所有的保留字（32个）：
    and    as    assert
    break
    class    continue
    def    del
    elif    else    except    exec
    finally    for    from
    global
    if    import    in    is    
    lambda
    nonlocal    not
    or
    pass     print
    raise    return
    try
    while    with
    yield
    
2.标识符 

    标识符是用来识别变量、函数、类、模块和其他对象的名称。标识符可以包含字母、数字和下划线（ _ ）,但必须以非数字字符开始。
    字母目前只允许使用 ISO-Latin 字符集中的字符 A~Z 和 a~z。由于标识符是区分大小写的，所有 FOO 和 foo 是两个不同的标识符。
    诸如$、%和@等特殊符号不允许出现在标识符中。另外，像if、else 和 for 这样的单词是保留字，也不能用作标识符名称（32）。

以下划线开始或结束的标识符通常具有特殊含义。
    例如：
    1.以一个下划线开始的标识符(如 _foo )不能使用 from module import * 语句导入。
    2.前后均带有双下划线的标识符(如 __init__ )是为特殊方法保留的。
    3.而只有前面带有双下划线的标识符(如 __bar )则用于实现私有的类成员。
    4.一般用途的标识符应避免使用以上几种格式。
    
   
    
