#datetime模块包含了几个对象和方法，用来表示绝对日期和时间以及相对日期和时间。
          #相关的值被用来计算时间之间的差别，从而避免你必须在基于秒的数值上做混乱的运算。

#datetime模块使用的主要的类有date、time和datetime。


---------------------------------------------------------------------------------------------
datetime和time：对象可以通过将它们的timezone特性设置为timezone对象来考虑时区的作用。
               如果要做复杂的时间处理，可能需要子类化timezone类来提供任何需要的复杂算法。
               
datetime：对象是date对象和time对象的结合，支持两个对象方法的结合。
          另外也包括自己的其他方法。
                    例如：
                        初始化为当前日期和时间的now()类方法。
                        接受date和time对象作为参数返回一个组合的拥有同样值的combine()类方法。
                        
               
---------------------------------------------------------------------------------------------
timedalta：另一个可能也是最有用的对象类型，它用于处理时间计算或相对时期，比如一年或一个月的时间间隔。
           datetime模块使用timedelta对象支持许多基于时间的计算，包括加法、减法、时间差和一个数的乘法。
           以及各种形式的除法
---------------------------------------------------------------------------------------------
date:可以通过传入年、月和日的值来初始化data对象（这几个值是必须传入的）。
     传入小时、分钟和秒的值来初始化对象（这几个值是可选的，默认都是0）。
---------------------------------------------------------------------------------------------
datetime：使用了所有年、月、日、小时、分钟和秒。
---------------------------------------------------------------------------------------------
date.today()方法:返回当天的日期。
---------------------------------------------------------------------------------------------
date.fromtimestamp()方法：接受一个秒的时间值作为参数
---------------------------------------------------------------------------------------------

